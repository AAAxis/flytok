{
  "name": "flytok-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for Flytok / Roamrez (chat push delivery, etc.)",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "~5.4.5"
  }
}
