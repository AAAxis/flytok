rules_version = '2';

// DEV mode: any signed-in user can read/write anything in this database.
// Replace with the per-collection rules in git history before going live.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
